<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Manager - Gerencie suas tarefas com estilo</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Poppins:wght@400;600;700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #1A1A1A;
            color: #FFFFFF;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: 
                repeating-linear-gradient(45deg, transparent, transparent 35px, rgba(255, 0, 110, 0.03) 35px, rgba(255, 0, 110, 0.03) 70px),
                repeating-linear-gradient(-45deg, transparent, transparent 35px, rgba(0, 201, 255, 0.03) 35px, rgba(0, 201, 255, 0.03) 70px);
            z-index: 1;
        }

        .container {
            max-width: 1200px;
            width: 100%;
            padding: 40px 20px;
            position: relative;
            z-index: 2;
        }

        .hero {
            text-align: center;
            margin-bottom: 80px;
        }

        h1 {
            font-family: 'Bebas Neue', cursive;
            font-size: 120px;
            letter-spacing: 5px;
            margin-bottom: 20px;
            background: linear-gradient(45deg, #FF006E, #00C9FF, #FFD600, #00FF88);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-transform: uppercase;
            animation: gradient 3s ease infinite;
            background-size: 200% 200%;
        }

        @keyframes gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .tagline {
            font-size: 24px;
            color: #999;
            margin-bottom: 50px;
            letter-spacing: 2px;
        }

        .cta-buttons {
            display: flex;
            gap: 30px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            padding: 20px 50px;
            border-radius: 15px;
            text-decoration: none;
            font-weight: 700;
            font-size: 18px;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 2px;
            position: relative;
            overflow: hidden;
            display: inline-block;
        }

        .btn-primary {
            background: linear-gradient(45deg, #FF006E, #00C9FF);
            color: white;
            box-shadow: 0 10px 30px rgba(255, 0, 110, 0.3);
        }

        .btn-primary::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .btn-primary:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 15px 40px rgba(255, 0, 110, 0.5);
        }

        .btn-primary:hover::before {
            width: 400px;
            height: 400px;
        }

        .btn-secondary {
            background: transparent;
            color: #00FF88;
            border: 3px solid #00FF88;
            box-shadow: 0 10px 30px rgba(0, 255, 136, 0.2);
        }

        .btn-secondary:hover {
            background: rgba(0, 255, 136, 0.1);
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 15px 40px rgba(0, 255, 136, 0.4);
        }

        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
            margin-top: 100px;
        }

        .feature-card {
            background: #2D2D2D;
            padding: 40px;
            border-radius: 20px;
            text-align: center;
            transition: all 0.3s ease;
            border: 3px solid transparent;
            position: relative;
            overflow: hidden;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: linear-gradient(90deg, #FF006E, #00C9FF, #FFD600, #00FF88);
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            border-color: #00C9FF;
            box-shadow: 0 20px 50px rgba(0, 201, 255, 0.3);
        }

        .feature-card:hover::before {
            transform: scaleX(1);
        }

        .feature-icon {
            font-size: 60px;
            margin-bottom: 20px;
        }

        .feature-title {
            font-family: 'Bebas Neue', cursive;
            font-size: 28px;
            letter-spacing: 2px;
            margin-bottom: 15px;
            color: #FFFFFF;
            text-transform: uppercase;
        }

        .feature-description {
            color: #999;
            line-height: 1.8;
        }

        .floating-shapes {
            position: absolute;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            pointer-events: none;
            z-index: 1;
        }

        .shape {
            position: absolute;
            opacity: 0.1;
            animation: float 20s infinite linear;
        }

        .shape:nth-child(1) {
            top: 20%;
            left: 10%;
            font-size: 80px;
            color: #FF006E;
            animation-delay: 0s;
        }

        .shape:nth-child(2) {
            top: 60%;
            right: 10%;
            font-size: 100px;
            color: #00C9FF;
            animation-delay: 5s;
        }

        .shape:nth-child(3) {
            bottom: 20%;
            left: 30%;
            font-size: 60px;
            color: #FFD600;
            animation-delay: 10s;
        }

        .shape:nth-child(4) {
            top: 40%;
            right: 30%;
            font-size: 90px;
            color: #00FF88;
            animation-delay: 15s;
        }

        @keyframes float {
            0% {
                transform: translateY(0) rotate(0deg);
            }
            50% {
                transform: translateY(-20px) rotate(180deg);
            }
            100% {
                transform: translateY(0) rotate(360deg);
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .hero, .features {
            animation: fadeInUp 1s ease-out;
        }

        .features {
            animation-delay: 0.3s;
            animation-fill-mode: both;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 60px;
            }
            
            .tagline {
                font-size: 18px;
            }
            
            .cta-buttons {
                flex-direction: column;
                align-items: center;
            }
            
            .btn {
                width: 100%;
                max-width: 300px;
            }
        }
    </style>
</head>
<body>
    <div class="floating-shapes">
        <div class="shape">▲</div>
        <div class="shape">●</div>
        <div class="shape">■</div>
        <div class="shape">◆</div>
    </div>

    <div class="container">
        <div class="hero">
            <h1>Task Manager</h1>
            <p class="tagline">Gerencie suas tarefas com estilo urbano</p>
            
            <div class="cta-buttons">
                <a href="/login" class="btn btn-primary">Fazer Login</a>
                <a href="/cadastro" class="btn btn-secondary">Criar Conta</a>
            </div>
        </div>

        <div class="features">
            <div class="feature-card">
                <div class="feature-icon">📋</div>
                <h3 class="feature-title">Organize Projetos</h3>
                <p class="feature-description">
                    Crie e gerencie múltiplos projetos com facilidade, 
                    mantendo tudo organizado em um só lugar.
                </p>
            </div>

            <div class="feature-card">
                <div class="feature-icon">✅</div>
                <h3 class="feature-title">Acompanhe Tarefas</h3>
                <p class="feature-description">
                    Defina prioridades, prazos e acompanhe o progresso 
                    de cada tarefa em tempo real.
                </p>
            </div>

            <div class="feature-card">
                <div class="feature-icon">💬</div>
                <h3 class="feature-title">Colabore</h3>
                <p class="feature-description">
                    Adicione comentários e mantenha toda a equipe 
                    alinhada sobre o andamento dos projetos.
                </p>
            </div>
        </div>
    </div>
</body>
</html>